# Project 1: nested compositor foundation — handoff

Living document for Project 1. Update it after every ticked task in
[../TASKS.md](../TASKS.md); finish it in P1-T11.

## Context

Earlier sessions produced a libwayland-server skeleton (compositor, `wl_shell`,
seat with evdev input, X11/EGL window, launcher) that compiles but cannot show
a real client. See [../STATUS.md](../STATUS.md) for the verified state and
[../decisions/02-nested-io-and-verification.md](../decisions/02-nested-io-and-verification.md)
for why the shell protocol, input source, and verification approach changed.

## Done so far

| Task | Evidence |
| --- | --- |
| P1-T01 headless harness, socket naming, exit timer | `meson test -C build smoke-headless` |
| P1-T02 test client and globals test | `meson test -C build client-globals` |
| Launcher: correct socket, inherited runtime dir, `execvp` argv, `DISPLAY` unset | manual: `--headless --launch "./build/mansion-test-client --exit-after-ms 200"` connects (compositor log) |

## Next task

P1-T03 (xdg-shell). See "Next task" in `../STATUS.md` for the continuation point.

## Implementation notes for the remaining tasks

- **Protocol generation.** `wayland-scanner server-header xdg-shell.xml
  xdg-shell-server-protocol.h`, `wayland-scanner private-code xdg-shell.xml
  xdg-shell-protocol.c`; the client needs `client-header` too. In Meson use
  `custom_target` with `generator`-style outputs and add the `.c` to both
  executables. Compile the generated C as C, not C++.
- **Configure handshake.** A toplevel is "mapped" only after the client acks a
  configure and commits a buffer. Send `xdg_toplevel.configure(width, height,
  states)` followed by `xdg_surface.configure(serial)`; keep the last serial to
  validate `ack_configure`.
- **Buffers.** `wl_shm_buffer_get(resource)` gives width/height/stride/format.
  Call `wl_shm_buffer_begin_access`/`end_access` around reads. Release the
  previous buffer after the new one is committed.
- **Frame callbacks.** Collect `wl_surface.frame` callback resources per
  surface; after each render tick call `wl_callback_send_done(cb, ms)` and
  `wl_resource_destroy(cb)`.
- **Listeners.** libwayland listener structs must be static or otherwise
  outlive the resource.
- **Two roundtrips.** A client sees seat name/capabilities on the roundtrip
  after binding, not the one that delivered the global.
- **Headless GL.** `eglGetPlatformDisplayEXT(EGL_PLATFORM_SURFACELESS_MESA,
  EGL_DEFAULT_DISPLAY, NULL)` then a pbuffer surface; read pixels with
  `glReadPixels(GL_RGBA)` and write P6 PPM. If EGL is unavailable, run without
  a renderer and let the screenshot flag print a clear error.

## Limitations to carry into later handoffs

- Keyboard layout comes from `XKB_DEFAULT_*` env or `us`; not from the host.
- Host shortcuts grabbed by the host compositor (Steam/KDE) cannot be captured.
- No `weston-terminal` in the container until a person installs `weston`.

## Human acceptance (P1-T10)

Not observed. Procedure will live in `docs/ACCEPTANCE.md`.
